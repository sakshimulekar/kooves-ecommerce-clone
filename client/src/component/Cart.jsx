import { Box, Heading } from '@chakra-ui/react'
import React from 'react'

const Cart = () => {
    console.log('cart')
  return (
    <Box mt={50} border={'1px solid'}>
      <Heading mt={50}>
      history.ts:487 You should call navigate() in a React.useEffect(), not when your component is first rendered.history.ts:487 You should call navigate() in a React.useEffect(), not when your component is first rendered.history.ts:487 You should call navigate() in a React.useEffect(), not when your component is first rendered.history.ts:487 You should call navigate() in a React.useEffect(), not when your component is first rendered.history.ts:487 You should call navigate() in a React.useEffect(), not when your component is first rendered.history.ts:487 You should call navigate() in a React.useEffect(), not when your component is first rendered.history.ts:487 You should call navigate() in a React.useEffect(), not when your component is first rendered.history.ts:487 You should call navigate() in a React.useEffect(), not when your component is first rendered.history.ts:487 You should call navigate() in a React.useEffect(), not when your component is first rendered.
      </Heading>
    </Box>
  )
}

export default Cart
